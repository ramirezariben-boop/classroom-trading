// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  eslint: {
    // ❗ Permite que el build en Vercel pase aunque haya warnings/errores de ESLint
    ignoreDuringBuilds: true,
  },
  typescript: {
    // ❗ Permite build aunque haya errores de tipo (TS). Úsalo para desbloquear y luego corrige.
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
